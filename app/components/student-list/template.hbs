<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th>
        UCID
      </th>
      <th>
        Name
      </th>
      <th>
        Major
      </th>
    </tr>
  </thead>
  <tbody>
    {{#each students as |student|}}
    {{#link-to 'students.edit' student.id tagName="tr"}}
      <td>
        {{ student.ucid }}
      </td>
      <td>
        {{ student.first-name }} {{ student.last-name }}
      </td>
      <td>
        {{ student.major }}
      </td>
    {{/link-to}}
    {{/each}}
  </tbody>
</table>
